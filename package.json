{
  "name": "backlog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "deno fmt --check && deno lint && deno check mod.ts && deno test",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "test": "deno test"
  }
}
